{
  "name": "my-node-express-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "compileTs" : "node dist/server.js",
    "build" : "tsc && npm run compileTs",
    "local" : "node build/server.js"
  },
  "scriptsComments": {
    "start": "this will run node on a remote website/server",
    "local": "local cmd will run node on a server.js file located in the build directorty NOTE** I have build in my gitignore so if you want to use local cmd please make a build directory and then run npm build. Now you can run nmp local"
  },
  "dependencies": {
    "@types/express": "^4.17.14",
    "@types/mocha": "^10.0.0",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "express-generator": "^4.16.1",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "mocha": "^10.1.0",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.21",
    "nodemon": "^2.0.15",
    "ts-node": "^10.9.1",
    "typescript": "^4.5.4"
  }
}
